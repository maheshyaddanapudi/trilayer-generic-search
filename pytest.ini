[pytest]
asyncio_mode = auto
addopts = --cov=src --cov=plugins --cov-report=term-missing --cov-fail-under=100 -m "not integration"
testpaths = tests
markers =
    integration: requires live Docker services (neo4j + postgres)
